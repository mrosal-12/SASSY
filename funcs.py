from statistics import mean, median, mode

# write functions that the calculations section can use
# in order to work properly, functions must take in an array of metrics and return a single value
# values can be of type str, int, or bool
# it should be up to the user to match types

all_funcs = {
    "mean": mean,
    "median": median,
    "mode": mode,
    "max": max,
    "min": min,
}

